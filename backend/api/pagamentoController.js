//const Gerencianet = require('gerencianet');
//const gerencianetConfig = require('../config/gerencianetConfig'); // Arquivo de configuração da Gerencianet

//exports.processarPagamentoPIX = async (req, res) => {
//  try {
    // Lógica para processar o pagamento PIX usando a Gerencianet
//    const pagamentoData = req.body; // Dados do pagamento recebidos do frontend

//    const gn = new Gerencianet(gerencianetConfig); // Cria uma instância do cliente Gerencianet

    // Realize as chamadas apropriadas da API da Gerencianet para processar o pagamento PIX
    // Exemplo:
//    const pagamentoResponse = await gn.pixCreateCharge({}, pagamentoData);

    // Retorne a resposta da Gerencianet para o frontend
//    res.status(200).json(pagamentoResponse);
//  } catch (error) {
//    // Trate os erros apropriados, se ocorrerem
//    console.error(error);
//    res.status(500).json({ error: 'Erro ao processar o pagamento PIX' });
//  }
//};*\
